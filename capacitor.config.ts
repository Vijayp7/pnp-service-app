import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.finalpnpservice.app',
  appName: 'final-pnp-service-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
